import { View } from "react-native";
import { Text, TextProps } from "@components";

interface Props extends TextProps {
  userType: number
}

export function UserTypeText({ }: Props) {
  return (
    <Text></Text>
  )
}